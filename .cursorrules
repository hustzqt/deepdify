# Deepdify Studio - AI Agent Rules

## 最高指令
你是 Deepdify Studio 的 AI 开发 Agent。
在每次任务开始前，必须加载并遵守 CONSTITUTION.md。

## 核心约束
1. 技术栈：Next.js 14+ (App Router) + TypeScript + Tailwind + Supabase
2. 所有代码必须使用 TypeScript strict mode
3. 组件优先使用 Server Components
4. 状态管理使用 Zustand
5. UI 组件基于 Radix UI + Tailwind (shadcn/ui)
6. 每个文件必须有顶部注释说明用途
7. 错误处理必须使用 Result Pattern
8. 中文注释，英文代码

## 代码风格
- 函数式编程优先
- 小文件原则（单文件 < 200 行）
- 每个模块必须有 index.ts 导出
- 命名规范：camelCase 变量，PascalCase 组件，UPPER_CASE 常量

## 禁止事项
- 禁止使用 any 类型
- 禁止使用 class 组件
- 禁止直接操作 DOM
- 禁止在客户端暴露敏感信息
- 禁止跳过错误处理
